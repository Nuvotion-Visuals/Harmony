import React from 'react'
import styled from 'styled-components'

interface Props {
  
}

const index = ({ }: Props) => {
  return (<S.index>
    Space
  </S.index>)
}

export default index

const S = {
  index: styled.div`
    
  `
}