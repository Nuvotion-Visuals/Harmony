version: "2"
services:
  filebrowser:
    image: hurlenko/filebrowser
    user: "${UID}:${GID}"
    ports:
    - 8999:8080
    volumes:
    - ../Lexisave/data/youtube/:/data/youtube
    - ../models/stable-diffusion/output/invoke:/data/invoke
    - ../lexi-studio-api/public/uploads/:/data/uploads
    - ./data/filebrowser/CONFIG_DIR:/config
    environment:
    - FB_BASEURL=/filebrowsers
    restart: always